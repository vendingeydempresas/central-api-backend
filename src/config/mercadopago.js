// config/mercadopago.jsconst fetch = require('node-fetch');  // Asegúrate de tener instalada la librería node-fetchconst ACCESS_TOKEN = process.env.MERCADOPAGO_ACCESS_TOKEN; // Reemplaza con tu token real// Función para crear el link de pago en Mercado Pagoconst crearLinkMercadoPago = async ({ title, quantity, price, description, external_reference }) => {  try {    const response = await fetch("https://api.mercadopago.com/checkout/preferences", {      method: "POST",      headers: {        "Content-Type": "application/json",        "Authorization": `Bearer ${ACCESS_TOKEN}`,      },      body: JSON.stringify({        items: [{          title,          quantity: Number(quantity),          currency_id: "CLP",          unit_price: Number(price),          description,          external_reference,        }],        external_reference,      }),    });    if (!response.ok) {      const errorBody = await response.text();      console.error("Error en Mercado Pago API:", errorBody);      throw new Error(errorBody);    }    const data = await response.json();    return { init_point: data.init_point };  } catch (error) {    console.error("Error interno en API:", error);    throw new Error(error.message);  }};module.exports = { crearLinkMercadoPago };